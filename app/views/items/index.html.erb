<% if params[:merchant_id].nil? %>
  <% @items.each do |item| %>
    <%= image_tag item.image, alt: 'product image' %>
    <%= item.name %>
    <%= item.description %>
    <%= item.price %>
    <%= item.status %>
    <%= item.inventory %>
    <%= link_to item.merchant.name, "/merchants/#{item.merchant_id}" %>
  <% end %>
<% else %>
  <%= render "merchant_items_index" %>
<% end %>